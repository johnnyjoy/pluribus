package pgtextsearch

// seedStatements lists plausible governance-style memories by rough class.
// Variants and tag assignment happen in seed_build.go.
var seedStatements = map[string][]string{
	"constraint": {
		"Do not use SQLite for production writes under concurrent ingest.",
		"All durable project data must use Postgres; SQLite is not permitted for authoritative state.",
		"Require idempotency keys on outbound webhooks before at-least-once delivery.",
		"Streaming decode is required for large JSON imports to reduce peak memory during batch jobs.",
		"Cross-region replication must use explicit version vectors; last-write-wins is forbidden for money.",
		"PII must not be logged at info level; use structured redaction for identifiers.",
		"Deploy migrations only through the approved pipeline; manual DDL on production is blocked.",
		"Feature flags must default off; gradual rollout requires staged cohorts and kill switches.",
		"Secrets belong in the managed vault; never embed long-lived credentials in repo configs.",
		"Public API pagination must be cursor-based for large tables; offset-only pagination is disallowed beyond 10k rows.",
		"Background workers must respect lease TTL; silent infinite retries are not allowed.",
		"Client caches must honor ETag; unconditional GET storms during incidents are unacceptable.",
		"Schema changes require compatibility window; additive-first, then backfill, then flip reads.",
		"Binary artifacts must be content-addressed; mutable latest tags are not a release mechanism.",
		"TLS termination must use modern ciphers; legacy TLS1.0 endpoints are not supported.",
	},
	"failure": {
		"Retry without idempotency caused duplicate records in downstream sync during warehouse lag.",
		"Badge update ordering is not guaranteed during warehouse lag; UI showed stale counts for minutes.",
		"A hot loop in the poller pegged CPU after a bad deploy; circuit breaker was missing.",
		"Partial batch commit left half the rows visible; users saw inconsistent totals until manual repair.",
		"OOM during JSON import killed the worker mid-file; no checkpointing meant full restart from byte zero.",
		"Duplicate event IDs were accepted because dedup cache TTL was shorter than retry window.",
		"Clock skew between services broke lease validation; nodes fought for the same partition.",
		"Large SELECT * queries saturated IOPS during peak; missing covering index amplified blast radius.",
		"Client backoff was too aggressive; thundering herd recovered into another outage window.",
		"Migration ran twice in overlapping regions; unique constraint caught it but blocked deploy for hours.",
		"Stale read from read replica returned deleted rows; session routing ignored replication lag hints.",
		"Webhook signature verification used wrong secret rotation order; partner traffic was rejected.",
		"Memory leak in connection pool exhausted fds; process restart was the only mitigation.",
		"CSV parser treated embedded newlines as row breaks; data landed in wrong columns silently.",
		"Feature flag cache never invalidated; users saw toggled features flip back randomly.",
	},
	"pattern": {
		"Prefer bounded queues with backpressure instead of unbounded in-memory buffers for ingest.",
		"Use streaming decode for large JSON imports to reduce peak memory and improve tail latency.",
		"Operational dashboards should separate user-visible errors from internal retry noise.",
		"Canary analysis should compare error budget burn, not just average latency.",
		"Dark launch new ranking behind shadow traffic before promoting to production cohorts.",
		"Treat idempotency keys as first-class headers; retries are inevitable in distributed systems.",
		"Use synthetic probes for dependency health; passive metrics miss partial brownouts.",
		"Batch writes with explicit chunk sizes; giant transactions stall replication and vacuum.",
		"Prefer deterministic replay fixtures for incident drills; ad-hoc scripts rot quickly.",
		"Log correlation IDs across service boundaries; paging without trace context wastes time.",
		"Keep kill switches coarse enough to operate under stress; too many flags confuse operators.",
		"Automate rollbacks for failed migrations; humans typing SQL under pressure is a risk.",
		"Cache embeddings offline for static corpora; online embedding storms are expensive.",
		"Normalize text before lexical match in hybrid pipelines; punctuation noise hurts recall.",
		"Document blast radius per dependency; on-call should know what fails closed vs open.",
	},
	"decision": {
		"We chose Postgres over SQLite for the control plane because of concurrent writers and HA needs.",
		"We will adopt RRF-style fusion for hybrid recall after lexical and semantic candidates exist.",
		"We postponed cross-region active-active until conflict resolution semantics are defined.",
		"We selected BM25 via pg_textsearch for lexical candidates to complement vector recall.",
		"We will keep canonical memory rows authoritative; search indexes remain rebuildable projections.",
		"We rejected embedding-only retrieval for compliance queries where exact phrases matter.",
		"We will store applicability and authority on memory rows; rankers may consume but not own them.",
		"We agreed to rebuild lexical indexes from canonical rows after schema experiments.",
		"We will cap candidate lists before expensive rerankers to protect tail latency.",
		"We chose explicit projection tables over mutating memories for BM25 document text.",
		"We will tag operational memories distinctly from product constraints for filtering tests.",
		"We decided nightly reindex is acceptable for early evaluation; incremental comes later.",
		"We will fail eval pipelines loudly when extension or index prerequisites are missing.",
		"We prioritized reversible automation over manual SQL for this exploration branch.",
		"We will measure lexical latency separately from end-to-end recall compile time.",
	},
	"state": {
		"Warehouse sync is currently 2–5 minutes behind primary during peak; plan queries accordingly.",
		"Embedding provider quota resets at 00:00 UTC; bulk jobs should shard across the window.",
		"On-call rotation includes one backup for recall pipeline incidents this quarter.",
		"Staging cluster runs pg18 with pgvector; lexical extension evaluation uses a branch overlay.",
		"Redis eviction policy is volatile-lru for session cache; do not assume durability there.",
		"Object storage lifecycle moves cold logs after 30 days; hot retrieval stays in the log stack.",
		"CI regression uses stock pgvector image; pg_textsearch eval uses the custom Postgres image.",
		"Load test environment resets weekly; do not treat IDs as stable across runs.",
		"Feature branch databases are disposable; tear down volumes when experiments finish.",
		"Current SLO for search p95 is not finalized; benchmarking is explicitly pre-release.",
		"Operator runbooks for index rebuild live next to rollback docs for the lexical experiment.",
		"Memory projection row counts should match seeded canonical rows after successful backfill.",
		"BM25 index build time is logged during eval for rough capacity planning.",
		"Eval seed uses deterministic tags for verification; do not reuse tags in production data.",
		"Hybrid ranking weights are not tuned yet; lexical eval is intentionally isolated.",
	},
}
