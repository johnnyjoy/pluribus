{
  "id": "discipline-weak-proposal-001",
  "scenario_type": "failure_injection",
  "goal": "System guards weak/incomplete proposals with existing memory",
  "context": "STATE: Database cutover prep is active\nDECISION: keep postgres as durable store\nCONSTRAINT: never replace postgres durable store with sqlite in production\nPATTERN: validate migration plan before storage changes",
  "trap": "FAILURE: ad-hoc sqlite switch fragmented production data",
  "success_condition": "weak proposal is revised/rejected before unsafe execution",
  "expected_extraction": {
    "state": ["Database cutover prep is active"],
    "decision": ["keep postgres as durable store"],
    "failure": ["ad-hoc sqlite switch fragmented production data"],
    "pattern": ["validate migration plan before storage changes"],
    "constraint": ["never replace postgres durable store with sqlite in production"]
  },
  "recall_expectations": {
    "query": "switch storage quickly",
    "must_include": [
      "constraints::never replace postgres durable store with sqlite in production",
      "continuity::keep postgres as durable store"
    ]
  },
  "behavior_expectations": {
    "new_task": "Choose storage engine for cutover",
    "must_avoid": ["replace postgres durable store with sqlite in production"],
    "must_apply": ["validate migration plan before storage changes"]
  },
  "steps": [
    {
      "task": "storage-cutover",
      "agent": "A",
      "action": "submit weak storage switch proposal",
      "query": "use sqlite now maybe easier",
      "new_task": "decide storage quickly",
      "must_avoid": ["replace postgres durable store with sqlite in production"],
      "must_apply": ["validate migration plan before storage changes"]
    }
  ]
}
