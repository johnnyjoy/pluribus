# Changelog

## Unreleased

### Documentation

- **Accurate wire shapes:** Public JSON for recall compile, run-multi, enforcement, drift check, curation, and minimal memory create paths is defined only by the `json` tags in `internal/*/types.go`. Prior draft notes about implicit workspace UUIDs on those bodies were removed as stale.
