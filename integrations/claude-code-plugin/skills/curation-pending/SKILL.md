---
name: curation-pending
description: Inspect pending memory curation when reviewing what needs human promotion or review. Use when the user asks about learnings queue, promotion, or advisory candidates—not for routine coding.
---

# Pending curation (Pluribus)

When appropriate MCP tools exist (e.g. **`curation_pending`**, **`curation_digest`**, **`curation_review_candidate`**):

1. Use them to list or review **candidates**, not to fabricate promotion decisions locally.
2. Treat outputs as **advisory** until the server materializes or promotes per doctrine.

If MCP is unavailable, say so once and fall back to HTTP/API docs in the Pluribus repo—do not invent queue contents.
