# Continue

1. MCP: **`mcp-config.example.json`** → Continue config.
2. **Native rules:** copy **`rules/pluribus.md`** → **`.continue/rules/pluribus.md`** ([Continue rules](https://docs.continue.dev/customize/deep-dives/rules)). Add **`snippets/context-prime.txt`** as another rule file or YAML rule entry.
3. Add **`skill.md`** if you want the step table inline.

**Canonical behavior:** [`pluribus-instructions.md`](../pluribus-instructions.md).

**`recall_context` → plan → act → `record_experience`** on substantive sessions.
