# AGENTS.md — Pluribus

Root **`AGENTS.md`** is optional when you already use **User rules** (Cursor Settings) with the same text—see **[`README.md`](README.md)** for the recommended **user-level** install.

Cursor **CLI agent** can read **`AGENTS.md`** at the repository root alongside **`.cursor/rules`** ([Cursor CLI docs](https://cursor.com/docs/cli/using)). Also optional if you rely only on **`.cursor/rules/pluribus.mdc`**.

1. Paste [`pluribus-instructions.md`](../pluribus-instructions.md).  
2. Paste **`snippets/context-prime.txt`**.
