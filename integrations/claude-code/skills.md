# Pluribus — Claude Code skill templates

Copy into project instructions or Claude Code skill packs.

1. **Ground** — `memory_context_resolve` → read bundle before plans.  
2. **Act** — implement with enforcement tools when proposals are risky (`enforcement_evaluate`).  
3. **Learn** — `mcp_episode_ingest` after meaningful work.  
4. **Deepen** — curation / contradictions only when debugging or promoting—optional.
