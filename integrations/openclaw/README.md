# OpenClaw

**`mcp-config.example.json`** here is **not** OpenClaw’s native file—it lists **values to register** via `openclaw mcp` (or your gateway). See [docs/integrations/openclaw.md](../../docs/integrations/openclaw.md).
