# Zed

1. MCP: **`mcp-config.example.json`** as template.
2. Paste **`agent-context.template.md`** into Zed **Agent** instructions (no universal repo path). Append **`snippets/context-prime.txt`**.
3. Add **`skill.md`** optionally.

**Canonical behavior:** [`pluribus-instructions.md`](../pluribus-instructions.md).

**`recall_context` → plan → act → `record_experience`** on substantive use.
