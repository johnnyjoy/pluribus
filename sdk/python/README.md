# pluribus-sdk (v0)

Minimal Python client for the core agent memory loop:

1. `recall_context(...)` before meaningful work  
2. Do the task (use the bundle)  
3. `record_experience(...)` after meaningful work

See `docs/sdk/python.md` in the main repository for usage guidance.
